name: Release

# Fires when you push a version tag like v1.0.0 or v1.2.3, or manually via the Actions UI.
# Regular commits and PRs are completely ignored.
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v1.2.3)'
        required: true

permissions:
  contents: read   # default for all jobs; the release job overrides with write

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: raven

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # Run tests first — if tests fail, no release is created
  # ─────────────────────────────────────────────────────────────────────────────
  test:
    name: Test before release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run workspace tests
        run: cargo test --workspace

  # ─────────────────────────────────────────────────────────────────────────────
  # Build release binaries for all supported platforms
  # ─────────────────────────────────────────────────────────────────────────────
  build:
    name: Build — ${{ matrix.artifact }}
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail_fast: false   # keep building other platforms even if one fails
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: raven-linux-x86_64

          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: raven-windows-x86_64.exe

          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: raven-macos-x86_64

          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: raven-macos-aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }} -p raven-cli

      # Rename binary to include platform name before uploading
      - name: Rename binary (Linux / macOS)
        if: matrix.os != 'windows-latest'
        run: |
          cp target/${{ matrix.target }}/release/${{ env.BINARY_NAME }} \
             ${{ matrix.artifact }}

      - name: Rename binary (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cp target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}.exe `
             ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  # ─────────────────────────────────────────────────────────────────────────────
  # Create the GitHub release and attach all binaries + checksums
  # ─────────────────────────────────────────────────────────────────────────────
  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write   # required to create releases and upload assets

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Generate SHA-256 checksums
        run: |
          cd artifacts
          find . -type f | sort | while read f; do
            sha256sum "$f" | sed 's|  \./|  |' >> ../checksums-sha256.txt
          done
          cat ../checksums-sha256.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
          name: RavenOSINT ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
          generate_release_notes: true
          body: |
            ## RavenOSINT ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}

            ### Download the right binary for your platform:

            | Platform | File |
            |---|---|
            | Linux (x86_64) | `raven-linux-x86_64` |
            | Windows (x86_64) | `raven-windows-x86_64.exe` |
            | macOS (x86_64) | `raven-macos-x86_64` |
            | macOS (Apple Silicon / ARM64) | `raven-macos-aarch64` |

            ### Quick start
            ```bash
            # Linux / macOS — make executable first
            chmod +x raven-linux-x86_64
            ./raven-linux-x86_64 --help

            # macOS Apple Silicon
            chmod +x raven-macos-aarch64
            ./raven-macos-aarch64 --help

            # Windows
            raven-windows-x86_64.exe --help
            ```

            See the [README](https://github.com/${{ github.repository }}/blob/main/README.md) for full usage.
          files: |
            artifacts/raven-linux-x86_64/raven-linux-x86_64
            artifacts/raven-windows-x86_64.exe/raven-windows-x86_64.exe
            artifacts/raven-macos-x86_64/raven-macos-x86_64
            artifacts/raven-macos-aarch64/raven-macos-aarch64
            checksums-sha256.txt
          draft: false
          prerelease: false
